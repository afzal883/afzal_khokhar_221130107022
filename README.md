# icon_perfumes

Follow the below steps to start the project:

Step 1:
-> install dependencies from requirements.txt using pip install -r requirements.txt.
Note: use Virtual Environment

Step 2:
-> Create "logs" dir in base direcotry.
-> inside logs folder create admin.log and error.log file.

Step 3:
-> Make migrations and migrate

Step 4:
-> Start the Project